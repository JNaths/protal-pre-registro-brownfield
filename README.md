# Portal de Pre-registro de Pacientes

Portal web de pre-registro de pacientes (formulario público) y consola de recepción.
Monorepo: React + TypeScript (web), Node.js + Express (api), esquemas Zod compartidos (validation), PostgreSQL vía Prisma.

> Repositorio inicializado. El código completo se incorpora mediante Pull Request.
